export const STORAGE_KEYS = {
  AUTH_TOKEN: 'sunny_whatsup_auth_token',
  AUTH_USER: 'sunny_whatsup_auth_user',
} as const;

export const COOKIE_KEYS = {
  AUTH_TOKEN: 'sw_token',
  AUTH_ROLE: 'sw_role',
} as const;
